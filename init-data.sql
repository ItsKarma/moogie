-- Basic database setup - this runs during PostgreSQL initialization
-- Sample data will be inserted via the API entrypoint after migrations

-- Create any additional database-level configurations here if needed
-- For now, just ensure the database is ready

SELECT 'Database initialized and ready for migrations' as status;
